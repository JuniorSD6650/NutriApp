// lib/core/theme/app_colors.dart
import 'package:flutter/material.dart';

class AppColors {
  // --- Colores Principales (Basados en 'SnapCalorie') ---
  
  // Naranja vibrante para botones, logo y elementos activos
  static const Color primary = Color(0xFFFF7043); // Un tono de Naranja/Coral

  // Color de fondo de la app (un gris muy claro)
  static const Color background = Color(0xFFF4F4F4); 
  
  // Color para las tarjetas o secciones (blanco)
  static const Color surface = Colors.white;

  // --- Colores de Texto y Secundarios ---
  
  // Para títulos y texto principal
  static const Color textPrimary = Color(0xFF333333); 
  
  // Para sub-títulos e iconos inactivos
  static const Color textSecondary = Color(0xFF8A8A8D); 

  // Color para bordes o divisores
  static const Color border = Color(0xFFE0E0E0);
}